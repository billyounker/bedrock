apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-cli-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-cli
  template:
    metadata:
      labels:
        app: api-cli
    spec:
      containers:
        - image: gcr.io/bedrock_test/bedrock_test-services-api-cli
          imagePullPolicy: Always
          name: api-cli
          env:
            - name: NODE_ENV
              value: "production"
            - name: MONGO_URI
              value: "mongodb://mongo:27017/bedrock_test_production"
            - name: APP_DOMAIN
              value: "bedrock-test.netlify.app"
            - name: APP_URL
              value: "https://bedrock-test.netlify.app"
            - name: POSTMARK_FROM
              value: "no-reply@bedrock-test.netlify.app"
            - name: POSTMARK_APIKEY
              value: "abc"
            - name: UPLOADS_STORE
              value: "gcs"
            - name: UPLOADS_GCS_BUCKET
              value: "bedrock_test-production-uploads"
          volumeMounts:
            - name: jobs-cache
              mountPath: /workdir/data
      volumes:
        - name: jobs-cache
          emptyDir: {}
